import React from "react";
import { Box, Typography, Container } from "@mui/material";

const ManagerPage = () => {
  return (
    <Container>
      <Box sx={{ padding: 3 }}>
        <Typography variant="h4" gutterBottom>
          Manager Dashboard (Empty for now)
        </Typography>
      </Box>
    </Container>
  );
};

export default ManagerPage;
