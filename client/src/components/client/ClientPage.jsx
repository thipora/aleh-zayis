import React from "react";
import { Box, Typography, Container } from "@mui/material";

const ClientPage = () => {
  return (
    <Container>
      <Box sx={{ padding: 3 }}>
        <Typography variant="h4" gutterBottom>
          Client Dashboard (Empty for now)
        </Typography>
      </Box>
    </Container>
  );
};

export default ClientPage;